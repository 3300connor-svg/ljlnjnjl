# Feature Tracker

## Completed

- Force reply prompt input (type your own prompt, no default)
- GO / CANCEL confirmation after prompt
- Balance endpoint fixed (`/api/v3/balance`)
- Step tracker removed, simple status messages
- Kling Standard / Pro buttons with accurate pricing
- Upscale + Seedream connection retry
- Button rename cleanup
- Custom prompt replaces instead of stacks

## Planned / Not Built

- ~~`character_orientation` fix for Kling~~ ✅ Done — analyzer auto-detects per-video; shown in Kling step
- ffmpeg post-processing step — subtle color grade + speed ramp for Instagram fingerprint evasion
- `_STEPS` tracker cleanup — removing Download/Extract from step list (deferred)
- Before/after AI detector analysis
- Multiple named model personas
- Instagram burner account integration
- Subscription / payment system
- App prototype build

## Discussed / Not Confirmed

- Slightly wider framing prompt tweak ("full shoulders visible, not cropped tight on face")
- Eye color consistency prompt fix ("both eyes identical color, matching light blue-green irises")
- 10s hard cap confirmed for Kling `video` orientation mode (reason to switch to `image`)
